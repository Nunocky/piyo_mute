{
  "name": "piyo-mute",
  "version": "1.0.0",
  "description": "ぴよ日記ミュート - piyo.hatelabo.jp で特定タグを非表示にする Chrome 拡張",
  "scripts": {
    "dev": "vite build --watch & vite build --config vite.content.config.ts --watch & npm run copy-static",
    "build": "vite build && vite build --config vite.content.config.ts && npm run copy-static",
    "copy-static": "cp manifest.json dist/ && cp src/popup.html dist/"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.325",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
